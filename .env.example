# Copy this file to .env and fill in secrets if a later milestone needs them.
# .env is gitignored; .env.example is safe to commit.
#
# P0 calibrates costs from own broker fills. Polygon and CBOE history are
# authorized at P0 only. Do not put any other vendor on the critical path
# of a first test.
